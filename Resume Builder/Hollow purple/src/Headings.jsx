import React from "react";
import "./resume-global.css";

const Headings = ({ titleX }) => {
  return (
    <h1 className="resume-sectionX">
      {titleX}
    </h1>
  );
};

export default Headings;
